class AppConfig {
  static const localhost = 'http://localhost:3001';
  static const login = localhost + '/auth';
  static const user = localhost + '/users';
  static const cart = localhost + '/cart';
}
